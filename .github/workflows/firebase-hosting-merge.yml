name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Auto Generate Local list.json in Every Single Directory
        run: |
          # 💡 深度掃描全站所有的子目錄，在每一個資料夾底下各自長出一個專屬的 list.json
          node -e "
            const fs = require('fs');
            const path = require('path');
            
            function traverseAndGenerate(dirPath) {
              const files = fs.readdirSync(dirPath).filter(f => !f.startsWith('.'));
              fs.writeFileSync(path.join(dirPath, 'list.json'), JSON.stringify(files, null, 2));
              
              files.forEach(file => {
                const fullPath = path.join(dirPath, file);
                if (fs.statSync(fullPath).isDirectory()) {
                  traverseAndGenerate(fullPath);
                }
              });
            }
            
            const rootFiles = fs.readdirSync('.').filter(f => !f.startsWith('.') && f !== 'firebase.json');
            rootFiles.forEach(file => {
              if (fs.statSync(file).isDirectory()) {
                traverseAndGenerate(file);
              }
            });
            console.log('🎉 200+ 個資料夾的專屬 list.json 批量全自動生成完畢！');
          "

      - name: Generate Perfect Clean firebase.json in Memory
        run: |
          # 💡 核心黑科技：開啟原生多站點靜態映射，告訴 Firebase 當遇到讀取目錄的結尾斜線請求時，自動在背景把當前目錄下的 list.json 塞給它！
          cat << 'EOF' > firebase.json
          {
            "hosting": [
              {
                "site": "pandapandaai",
                "public": ".",
                "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
                "cleanUrls": false,
                "trailingSlash": true,
                "rewrites": [
                  {
                    "source": "**/",
                    "destination": "list.json"
                  }
                ]
              }
            ]
          }
          EOF

      - name: Deploy to Firebase Hosting via Official Action
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PANDAPANDAAI }}
          channelId: live
          projectId: pandapandaai
