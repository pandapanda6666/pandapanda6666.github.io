2026/09/12 12:48:23
7index.html?v=1.5.3:944 Uncaught ReferenceError: key is not defined
每次進到模擬器都要重置一遍喔
抱歉！剛才在清除重複語法時，不小心連同 `keyup`（按鍵放開）事件裡的按鍵偵測一起刪掉了，這會導致放開按鍵時報錯而且無人機停不下來，我已經火速將它修復！

另外，關於「每次進到模擬器都要重置」的問題，我幫你徹底解決了：

1. **修復「無限重生」Bug**：之前即使無人機沒有摔壞，每次點擊「開始飛行」進去時都會判定成需要重新載入模型，這會導致無謂的等待和閃爍。現在只有在「真的摔壞解體」時才會重新載入機體。
2. **加入「自動存檔」機制 (LocalStorage)**：現在你**選擇的無人機款式**、**針對機體調校的數值（推力、傾角、轉向率）**，以及**遙控器模式（美國手/日本手）**，全部都會自動儲存在瀏覽器裡！下次就算完全關閉視窗再打開，你的專屬愛機和手感設定都會原封不動地保留著，不用再重新設定一遍了！

請再次按 `F5` 重新整理，試試看完美的飛行體驗吧！（備份已更新為版本 232）
1. 在 `index.html` 的 `keyup` 監聽器中補回 `const key = e.key ? e.key.toLowerCase() : '';`，修復 ReferenceError。
2. 修正 `initGamePhysics()` 中判定是否需要重生機體的邏輯，加上 `.length > 0` 條件，避免每次進入 `page-D` 時皆強制觸發 `selectDrone()` 重新載入模型。
3. 在 `index.html` 中實作 localStorage 自動存檔與讀取機制，包含 `uav_activeDrones` (各機體調校數值)、`uav_currentDroneId` (當前選擇的機體)、`uav_controlMode` (美國手/日本手)。解決重整網頁後設定遺失的問題。
4. 完成備份。
