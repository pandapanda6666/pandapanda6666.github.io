1.2026/08/12 12:18:15
2.標記是那個地圖預設的欸 (如圖) 還有要可以編輯文字 目前無法搜尋 右上角要顯示目前畫面的座標
3.我已經將畫布的標記工具替換成使用與地圖完全一模一樣的「藍色水滴圖釘」了！並新增了雙擊修改文字的功能。另外，右上角也會即時顯示座標，且定位搜尋功能也修復了。
4.
- 修正 `mouse:down` 中的標記邏輯，捨棄原本自行繪製的 SVG，改為載入原生地圖的 `marker-icon.png` 放入 Fabric 群組。
- 加入 `mouse:dblclick` 事件，當雙擊標記群組時，會跳出 `prompt` 供使用者修改標記上的文字（如果清空則刪除文字）。
- 在 `#ui-layer` 的右上角新增 `#map-center-coords` 區塊，綁定 `map.on('mousemove')` 即時顯示游標在地圖上的座標。
- 修復面板定位搜尋失敗的問題：由於原生 Leaflet Geocoder 有時內部呼叫會失敗，我將其改寫為使用標準的 `fetch` 直接打向 Nominatim API，並解析回傳的經緯度與地名。
