2026/09/07 17:50:46
所有設定皆可重置 ... UAV Simulator/tello:1 Failed to load resource: the server responded with a status of 404 ... Uncaught ReferenceError: toggleHover is not defined
又双叒抓到蟲了，我剛剛的程式碼搬移腳本竟然不小心把 `toggleHover` 跟 `toggleHeadless` 這兩個控制函數搬到了 `</html>` 標籤的外面！難怪瀏覽器會直接當作沒看到，導致找不到函數報錯。

然後那兩個 404 報錯是因為，Tello 跟 Soccer 我們是直接用程式碼生成的 (Procedural)，不需要讀取外部的 `.glb` 模型檔。但是原本的模型載入系統 (gltfLoader) 很笨，它傻傻地跑去找名為 `tello` 跟 `soccer` 的檔案，找不到才 fallback 回來。我現在已經幫它們加上快速通關 (Bypass) 了，直接跳過模型載入，就不會再出現 404 紅字了！

另外你要求的 **重置設定** 功能我也加上了！在「Rabboni 遙控器設定」面板標題旁邊，多了一顆紅色的 **「重置所有設定」** 按鈕，按下去就能瞬間把搖桿分配、控制方式和靈敏度全部變回預設值。

網頁 (版本 198) 已經推送到 Github，Rabboni 沒開的紅字請安心忽略。請再次 **Ctrl + F5 強制重新整理**，這次就真的完美無瑕了！
1. 修復 toggleHover/toggleHeadless 函數遺失問題：將其從 </body> 與 </html> 外部移回 <script> 區塊內。
2. 修復 404 報錯：修改 selectDrone() 邏輯，當偵測到 modelStr 為 tello 或 soccer 時，直接呼叫 buildProceduralDrone()，不再觸發 gltfLoader 導致網路請求報錯。
3. 新增重置功能：在 Rabboni 控制器設定面板的標題列增加「重置所有設定」按鈕，綁定 resetAllSettings() 函數將 UI 參數與資料庫回復預設。
