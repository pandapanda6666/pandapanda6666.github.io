1.2026/08/12 12:36:55
2.對話框像這樣 然後編輯模式下點兩下可以編輯 但是檢視模式不行喔
3.我已經完成了「完美還原原生地圖圖釘」的實作，並淘汰了所有系統原生的提示彈窗！
4.
- 新增 `#custom-dialog-overlay` 結構與 `uiAlert`, `uiConfirm`, `uiPrompt` 方法，並將 `index.html` 內所有的 `alert`, `prompt`, `confirm` 呼叫替換為自訂的美觀 UI 元件。
- 在 `btn-marker` 的 `mouse:down` 邏輯中，現在不再產生 `fabric.Image`，而是直接將座標存入 `appData.projects[i].nativeMarkers` 中，並透過原生的 `L.marker().bindPopup()` 渲染在 Leaflet 地圖上。
- 在 `btn-select` 工具且雙擊畫布的邏輯中，新增對 `nativeMarkers` 座標的攔截：如果雙擊位置鄰近某個原生標記，會跳出 `uiPrompt` 讓使用者編輯文字，符合「編輯模式下點兩下可編輯」。
- 在檢視模式（無 Fabric 事件攔截）下，使用者可以直接點擊標記，單純展開原生的 Popup 對話框，符合「檢視模式不能編輯」的防護要求。
- 將右上角座標邏輯從 `mousemove` (游標位置) 改為 `move`，並顯示 `map.getCenter()`，正確呈現畫面中心座標。
