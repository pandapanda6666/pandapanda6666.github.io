2026/09/10 13:09:35
有線連接勒？
右邊的控制項全都點不了
兩個問題都搞定了！

1. **控制項點不了的問題**：這是我剛才在重構 HTML 架構時，不小心讓不可見的背景全螢幕頁面（Z軸圖層）覆蓋到了按鈕上方，導致滑鼠點擊被攔截。我已經將操控介面的 `z-index` 提高並加上了 `pointer-events: auto`，現在按鈕都可以正常點擊了！

2. **USB 有線連接**：我已經把 Rabboni 的 USB 專屬解碼邏輯 (利用 `pywinusb`) 一併寫進去並重新打包了！新的 `UAV_Simulator.exe` 現在是一個**雙模連線**的超強後台：
   - 它會同時在背景偵測 **藍牙 (BLE)** 與 **USB 有線連接**。
   - 只要你插上線，它會自動抓取 HID 裝置、完成初始化並即時傳送資料到前端。
   - 這樣一來，不管你是用藍牙還是插線，完全不需要透過原本的 Flutter 應用程式，直接開這個模擬器就能玩！
1. 修復 HTML 中 `z-index` 與 `pointer-events` 的層級覆蓋問題，確保 `#ui-container` 等 UI 容器具備最高層級且能接收滑鼠事件。
2. 在 `run.py` 中引入 `rabboni` 模組的 USB 連線實作，並開設獨立的背景執行緒自動輪詢 `r.device.is_plugged()`，將解析完的資料與 BLE 邏輯並存，統一透過 Websocket 傳遞給前端。
3. 使用 PyInstaller 重新打包雙模直連版本的 `UAV_Simulator.exe`。
