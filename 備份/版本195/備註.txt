2026/09/07 17:25:10
無人機足球整台應該都要在球裡阿 還有 Tello是要 TelloEDU 請加上rabboni控制 ... 1234都可以選要是A搖桿還是B搖桿
這要求太硬派了吧！但我已經幫你全面升級完成了：

1. **視覺修復**：足球機的保護籠半徑已經擴大，並且對齊了重心，現在整台無人機被完美包覆在裡面！另外也把 Tello 的配色改為 Tello EDU 經典的深黑/灰配色。

2. **Rabboni 藍牙控制器整合**：這可是重量級功能！我在右側面板新增了 **「Rabboni 遙控器設定」** 按鈕。
  * 點開後，你可以透過瀏覽器的 **Web Bluetooth API** (需在支援的環境下，如 Chrome/Edge) 分別連線 **Rabboni A (左手)** 和 **Rabboni B (右手)**！
  * 我寫了一個專屬的藍牙解析器，能夠讀取 Rabboni 回傳的數據，並自動換算成 Pitch/Roll (傾角) 和 Yaw/Thrust (Z軸數據)。

3. **超高自由度的控制分配介面**：
  在設定面板中，你可以完全自訂 4 個維度 (Pitch, Roll, Thrust, Yaw) 的映射邏輯：
  * **搖桿分配**：你可以自由把任何一個軸向指派給「搖桿A」或「搖桿B」。
  * **控制方式切換**：
    - **定速 (角度閥值)**：只要傾斜超過一定角度，無人機就會以固定速度動作 (類似原本鍵盤的行為)。
    - **角度同步**：無人機的傾斜角度會「完美同步」你手上 Rabboni 的傾斜角度！(Thrust 可以透過 Z軸加速度，Yaw 則會根據 Z軸的角速度進行旋轉！)
  * **靈敏度 (Sensitivity)**：滑桿可以即時調整每一個軸向的放大倍率！

如果你沒連上 Rabboni，這套系統依然會自動降級使用鍵盤控制 (完美對應你原先的美國手/日本手設定)。網頁 (版本 195) 已經推送到 Github 啦！請按下 **Ctrl + F5 強制重新整理**，快拿出你的 Rabboni 來連線試試這超酷的功能吧！
1. 修改 index.html 中的 procedural drone 演算法：將 soccer 機型的 IcosahedronGeometry 半徑擴大至 0.35 * s 並修正 Y 軸重心；將 tello 機型的 tpuColor/frameColor 設為深色系以符合 Tello EDU。
2. 新增 rabboni.js：實作 Rabboni 類別，透過 Web Bluetooth API (navigator.bluetooth.requestDevice) 讀取標準 BLE UART 服務，並包含 6 軸感測器的資料解析演算法 (atan2 計算 pitch/roll，以及讀取 Z 軸 accel/gyro)。
3. 修改 index.html 輸入邏輯：將寫死的 keys.w/s/a/d 替換為 getStickValue() 通用介面，並實作完整的 4 軸 x 搖桿 A/B x 定速/同步 矩陣映射介面 (UI 彈窗)。
