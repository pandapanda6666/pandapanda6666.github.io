2026/09/12 17:09:24
例如旋轉方式直接同步了
對！因為之前我沒有把「姿態旋轉」的計算邏輯跟模擬器空間完全切開，導致你在機庫 (選單) 畫面的時候，如果你動了遙控器或鍵盤，機庫裡的飛機居然也會跟著轉動、傾斜，讓你覺得控制被「同步」了！

我現在已經徹底將控制邏輯給隔離：
1. **模擬器專屬控制**：你只有在進入「飛行模擬器」時，Rabboni 遙控器和鍵盤的控制訊號才會接入機體。
2. **機庫自動展示**：當你在機庫畫面挑選飛機時，無人機會不受遙控器干擾，只會**自動且優雅地在原地緩緩旋轉**，讓你可以 360 度無死角欣賞機身模型。

請按 `F5` 重新整理，現在機庫和飛行模擬已經是兩個完全獨立互不干擾的空間了！（備份已更新為版本 235）
1. 將 `droneGroup.rotation.set` 以及姿態內插計算 (`currentPitch`, `currentRoll`, `extraRoll`) 移入 `if (currentPage === 'page-D')` 區塊，避免遙控訊號污染機庫畫面。
2. 在 `else` 區塊 (機庫畫面) 中，強制將 `inputThrottle`, `inputPitch`, `inputRoll`, `inputYaw` 歸零，並設定 `droneGroup.rotation` 套用隨時間變化的 `performance.now()` 慢速 Y 軸自轉邏輯，實作機庫展示效果。
3. 完成檔案備份。
