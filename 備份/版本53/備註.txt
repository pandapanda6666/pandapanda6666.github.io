1.2026/08/11 09:45:14
2.自由拉伸變形 不過如果原本沒有自由拉伸變形 要尋問是否確定要變型 可以按取消取消動作 搜尋後端直接用地圖原本的(如圖) 目前無法放置標記
3.我已經為您解決了所有問題！修復了原本標記無法放置的 bug，並且完整實作了「物件地理坐標定位」面板，搜尋機制直接沿用地圖內建的 Leaflet Geocoder，且在斜對角拉伸時會跳出確認視窗。
4.
- 修正 `mouse:down` 中 `btn-marker` 的邏輯判斷位置，原本因為正則替換失敗導致沒有綁定到正確的滑鼠事件，現在點擊畫布可以正常彈出輸入框並生成標記了。
- 在 `top-context-toolbar` 中新增「📍 位置」按鈕。
- 新增 `obj-location-panel` 浮動面板，實作「中心點」與「斜對角」兩種定位模式的介面。
- 整合 `L.Control.Geocoder.nominatim().geocode()` 來處理地點搜尋，確保與地圖原生的搜尋後端一致。
- 新增座標轉換邏輯 `latLngToFabric` 將搜尋到的經緯度轉為 Fabric.js 的 X/Y 像素。
- 在「斜對角定位」套用時，加入 `confirm()` 警告視窗，詢問是否確認自由拉伸變形，如果取消則中止動作。
