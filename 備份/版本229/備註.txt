2026/09/11 17:47:25
我是說加上左翻滾和右翻滾(360度側向翻轉一圈)
了解，原來你要的是特技飛行（Acro / Flip）的 360 度翻滾！

我已經幫你加入了專屬的特技按鍵：
- 按下 **`Q` 鍵**：觸發 360 度**左側翻滾**
- 按下 **`E` 鍵**：觸發 360 度**右側翻滾**

因為物理引撆套用了真實的重力和推力邏輯，所以在翻滾過程中（機身倒置時），推力會朝下，機體會出現真實的「掉高（掉高度）」現象！要做出完美的特技翻滾，建議在按下 Q/E 之前稍微補一點油門拉高，翻過去的時候才會順暢不墜機喔！

我也一併更新了左側的操作說明面板。請按 `F5` 重新整理，立刻體驗 360 度翻滾的特技飛行吧！（備份已更新為版本 229）
1. 在 `index.html` 的事件監聽器中新增 `Q` (左翻滾) 與 `E` (右翻滾) 的按鍵偵測，並寫入 `flipState` 狀態標記。
2. 於 `animate` 迴圈內實作隨時間補間的 360 度角速度翻轉邏輯，每秒 12.0 弧度，翻轉滿一圈 (`2 * Math.PI`) 即自動停止。
3. 將特技翻滾的姿態疊加至 `droneGroup.rotation`，完美融合原本的物理推力向量，呈現真實翻滾會掉高的物理現象。
4. 更新控制說明介面文字，提示 Q / E 按鍵功能。
5. 完成備份。
