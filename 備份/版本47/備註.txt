1.2026/08/10 15:01:57
2.權選沒法用 而且沒有封鎖瀏覽器原生全選(全選文字)
3.非常抱歉！在之前第 43 版清理多餘大括號 `}` 時，我的清理腳本不小心誤刪了一大塊剛加好的功能（包含全選、右鍵拖曳地圖和 Ctrl+滾輪縮放）。我現在已經將它們全部補回來了！同時也加入了 `e.preventDefault()`，現在按下 `Ctrl+A` 時不會再觸發瀏覽器原生的全選文字功能了。
4.重新注入 `btn-select-all.onclick`、右鍵拖曳、Ctrl+滾輪縮放邏輯。並新增了 `keydown` 監聽器，當按下 `Ctrl+A` 且不在輸入框時，攔截原生事件 (`e.preventDefault()`) 並觸發畫布全選。
