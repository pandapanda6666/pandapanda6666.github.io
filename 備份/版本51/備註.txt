1.2026/08/10 16:53:27
2.不要有標記了(這樣如果有很多會很亂) 直接在專案清單點選可以移動到那裡
3.我已經為您移除了地圖上的所有專案標記圖示，讓地圖保持乾淨！同時也修改了「專案清單」的點擊行為，現在只要點擊清單中的專案，系統就會自動將視角移動到該專案的位置，並同時跳出專案選項視窗！
4.
- 移除 `renderProjects` 中建立與加入 marker (`L.marker`) 的相關邏輯。
- 修改 `renderProjectListUI` 中的 `onclick` 事件，從原先只開啟 modal (`openProjectModal(p)`) 改為先飛到該地點 (`map.flyTo([p.lat, p.lng], p.zoom)`) 再開啟 modal。
