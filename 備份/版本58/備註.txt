1.2026/08/12 12:55:01
2.Uncaught ReferenceError: map is not defined、標記也是要放在地圖中間 並且標記的中心點為下面最窄的那個點
3.我已經修復了 `map is not defined` 等一連串初始化錯誤！並且完美達成了您對標記的最新要求：現在點擊標記工具時，圖釘會自動放置在地圖畫面的正中央，且圖釘最窄的下尖端會精準對齊座標！
4.
- 修復 `map is not defined` 導致後續變數 `isPanningMap` 與 `fabricToLatLng` 無法初始化的問題：將 `editNativeMarkersLayer` 的初始化語法延後至 `const map = L.map(...)` 宣告之後執行。
- 修正 Leaflet 預設圖標路徑：透過 `L.Icon.Default.mergeOptions` 覆寫原生的圖釘圖片路徑 (包含 2x 與 shadow)，解決圖片載入異常導致錨點 (anchor) 偏移的問題，現在圖釘的最下端尖點會 100% 精準對齊實際座標。
- 修改 `btn-marker` 的點擊行為：現在按下「標記」工具按鈕時，不再需要於畫面上點擊滑鼠，而是會直接透過 `map.getCenter()` 取得畫面正中央的座標，跳出文字輸入框後，自動在畫面中心點放置原生地圖圖釘。
