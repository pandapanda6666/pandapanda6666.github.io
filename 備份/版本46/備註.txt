1.2026/08/10 14:57:09
2.儲存怎麼變返回了?
3.我已經為您修正了這個問題。之前在儲存專案的底層邏輯 (`saveProjectData`) 中，統一被設定為只要儲存成功就會呼叫 `cancelEdit()` 回到首頁。現在我把它修改成只有在「發布」時才會跳出編輯畫面，若是按下「儲存」(存為草稿)，則只會儲存進度並跳出提示，不會中斷您的編輯了！
4.修改 `saveProjectData` 函式，將 `cancelEdit()` 包裹在 `if (!isDraft)` 的判斷中。
