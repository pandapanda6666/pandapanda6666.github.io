2026/09/12 12:56:18
每次開始都必須重置所有的東西 包還ESC選單的顯示狀態
啊，我懂你的意思了！你是指每次從主選單或機庫點擊「起飛」進去模擬器時，遊戲不會自動還原成乾淨的狀態，連之前打開沒關掉的 ESC 選單跟警告彈窗都會卡在畫面上，導致你進去還要自己手動關掉跟重置，對吧？

我已經把這個流程全面升級了：

1. **選單自動收合**：現在只要你離開或進入模擬器，系統會強制將 ESC 暫停選單和墜機警告彈窗隱藏，保證你一進去就是乾淨的飛行畫面。
2. **物理姿態全面歸零**：過去重置時只把機體「位置」拉回原點，但如果機身是翻面的，重置後還是翻面的！現在加上了**傾角、旋轉 (Yaw)、翻滾狀態 (Flip)** 的徹底歸零，保證每次起飛無人機都是正面向前的完美待命狀態。
3. **自動開始計時**：現在點擊「起飛」進入畫面時，計時器會自動從 0 秒開始計算，不需要再手動按 `R` 鍵重置時間。

請按 `F5` 重新整理，現在你不管怎麼摔機、怎麼開選單，只要點「起飛」進去，一切都會是完美的重新開始！（備份已更新為版本 233）
1. 在 `index.html` 的 `navigateTo` 函數中，加入強制隱藏 `pause-menu` 與 `alert-modal` 的邏輯。
2. 將 `navigateTo('page-D')` 觸發的 `initGamePhysics()` 替換為 `startGameTimer()`，讓系統進入模擬器時能自動重置並啟動計時器。
3. 在 `initGamePhysics()` 中補上 `window.droneYaw = 0`、`flipState = 0` 以及 `droneGroup.rotation.set(0, 0, 0, 'YXZ')`，徹底修復重置時姿態未歸零的 Bug。
4. 完成備份。
