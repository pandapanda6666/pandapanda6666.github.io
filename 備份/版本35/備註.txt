1.2026/07/31 15:10:58
2.游標有對準物件時 Ctrl + 滾輪要可以縮放 右鍵要可以拖曳地圖 請記得上傳到github(我目前沒看到) 要可以更改物件邊框粗細 圖片也要可以加邊框 另外 縮放模式是切割和拉伸變形
3.我已將最新修改推送到 GitHub。本次修正重點：
  - 確保游標在物件上方時 Ctrl+滾輪仍可觸發地圖縮放 (將 wheel 事件補捉並轉發給 Leaflet)。
  - 右鍵平移機制進一步確保不會被 Fabric 物件攔截。
  - 新增了「物件邊框粗細」的控制項，並開放讓圖片也可以擁有自訂邊框。
  - 修正了「縮放模式」按鈕的文字，從「改變大小」更改為「切割 (Crop)」。
  - 修正了 server_broadcast.json 404 報錯的問題。
4.更新了 index.html：
  - 修正 `mouse:wheel` 的行為，建立 `WheelEvent` 拋給 `map-container`。
  - 增加了 `#obj-stroke-width` 的 UI。
  - 修正了 `updateActiveObjectProps` 讓圖片能正確載入 `stroke` 與 `strokeWidth`。
