2026/09/09 12:44:32
A改成R
沒問題！確實，原本設成 A 鍵會跟控制左右傾斜的 `A` 鍵 (Roll 左) 發生嚴重衝突，導致你只要一往左飛就會無限重置回原點。這是一個很要命的按鍵衝突！

我已經把遊戲畫面上的提示字樣，以及鍵盤監聽事件，全部從 `A` 改成 `R` (Restart / Reset) 了。

網頁 (版本 209) 已經更新並推送到 Github 啦！按 **Ctrl + F5** 重新整理一下，現在你可以安心用 `R` 鍵重置，放心用 `A` 鍵往左飛了！
1. 將 index.html 中的重置遊戲快速鍵從 `A` 修改為 `R`，以避免與鍵盤飛行的左飛 (Roll Left, A鍵) 發生衝突。
2. 將畫面上方的提示文字由「按下 A 鍵開始 / 重新開始」同步修正為「按下 R 鍵開始 / 重新開始」。
