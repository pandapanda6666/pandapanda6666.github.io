1.2026/08/12 10:01:04
2.要可以下載專案檔到電腦 並匯入
Uncaught TypeError: Cannot set properties of null (setting 'onclick')
3.我已經成功修復了 `onclick` 為 null 的報錯問題（這是因為前一次的 HTML 插入腳本漏掉了定位按鈕的 UI 結構）。同時，我也實作了完整的專案匯出與匯入功能。
4.
- 修正 `index.html`，正確在 `#top-context-toolbar` 中插入 `btn-obj-location` 按鈕。
- 在首頁專案清單上方的「新增專案」旁邊加入「匯入專案」按鈕 (`btn-import-project`) 及隱藏的 `<input type="file">`。
- 實作匯出邏輯：點擊專案設定面板的「匯出專案檔」時，會將當前專案資料 `JSON.stringify` 並透過 Blob/DataURI 下載到使用者電腦。
- 實作匯入邏輯：選取 JSON 檔案後，解析並賦予新的專案 ID 以避免衝突，若已登入則自動設定為專案作者，並根據權限自動設為「已批准」或「草稿」，最後儲存到雲端並平移地圖視角。
