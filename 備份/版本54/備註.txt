1.2026/08/11 09:54:20
2.要可以下載專案檔到電腦 並匯入
Uncaught TypeError: Cannot set properties of null (setting 'onclick') at non-existent-map/:481:57
3.我已經成功修復了 `onclick` null 的報錯，並且為系統加入了「專案匯出與匯入」的強大功能！現在您可以把辛苦繪製的專案下載成 JSON 檔案保存，也可以隨時把以前的專案匯入進來。
4.
- 修正 `btn-obj-location` 綁定 `onclick` 時 DOM 節點尚未被正確插入 HTML 的問題（修正了字串替換未匹配到的 divider 標籤）。
- 在首頁清單 `tab-home` 的「新增專案」按鈕旁邊，加入了一組 `<button id="btn-import-project">` 與隱藏的 `<input type="file">`，點擊後可上傳 `.json` 專案檔。
- 實作了匯入解析邏輯：讀取檔案後會自動產生新的亂數 `id` 防止衝突，並根據當前登入的使用者自動指派擁有權（如果是管理員匯入會自動 approved，一般使用者則自動進入待審核或草稿），接著自動重繪專案列表並將鏡頭飛往該專案地點。
- 實作 `btn-proj-export` 邏輯：從 `appData.projects` 中找到對應資料後，轉為 JSON 字串並觸發瀏覽器下載 `.json` 檔案。
