1.2026/08/10 15:38:06
2.回首頁不算返回嗎? 目前無法儲存(儲存幾次還是顯示「還有未儲存的變更，確定要返回嗎？」並且首頁也看不到) 請不要在有專案的地方放一個作者頭像 請改成點擊清單中的那個專案才會跳出選項 管理員要可以批准別人的 管理員的會自動批准
3.我已經修正了未儲存提示、移除了地圖上的作者頭像，並且將清單改為點擊後彈出視窗（您可以在彈出視窗中編輯、刪除或批准）。
4.
- 加入 `isDirty` 變數來追蹤畫布是否有未儲存的修改。只有當 `isDirty === true` 時，按下返回（或回首頁）才會跳出「未儲存」警告。儲存成功後會重置 `isDirty`。
- 修改 `renderProjects`，把地圖上的大頭貼圖示改為普通的圖釘地標 (`fa-map-marker-alt`)。
- 修改 `renderProjectListUI`，移除原本懸停才會出現的編輯/刪除按鈕，並改為點擊整個清單項目後，呼叫 `openProjectModal(p)` 開啟專案詳細視窗。
- 在 `openProjectModal` 中加入邏輯：如果使用者是該專案的作者或是管理員，則會在視窗內顯示「編輯專案」與「刪除專案」的按鈕，並與原有功能綁定。管理員本身就能看到批准按鈕。
