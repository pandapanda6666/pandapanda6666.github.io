1.2026/08/11 09:07:52
2.加上一個標記功能 可以在指定地點放置一個標記 可以自訂顏色 並且加上文字(標記就是現在的那種標記 但是直接在專案裡 而且可以放置多個)
3.我已經在繪圖工具列加入了全新的「標記」功能！選取後在畫布上點擊，會彈出輸入視窗讓您輸入想標記的文字。接著就會在畫布上產生一個包含「圖釘圖示」與「文字」的群組物件。您可以自由拖曳、放置多個，並且透過上方的顏色選擇器來自訂圖釘的顏色（填滿顏色）以及文字的顏色（外框顏色）。
4.
- 在 `#tab-tools` 工具列中加入 `<button id="btn-marker">`。
- 增加點擊按鈕的切換邏輯 `setActiveBtn('btn-marker')`。
- 攔截畫布的 `mouse:down` 事件：當選擇標記工具且點擊空地時，先呼叫 `prompt` 取得文字內容，接著產生一個 `fabric.Path` (採用 SVG 圖釘路徑) 以及 `fabric.Text`，組合成一個 `fabric.Group`，並標記 `isMapMarker: true` 加到畫布中。
- 修改上方功能列的更新邏輯與顏色修改邏輯：若選取的是 `isMapMarker` 的群組，變更填滿顏色時會更新圖釘的顏色，變更外框顏色時則更新文字的顏色。
