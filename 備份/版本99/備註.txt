1.2026/08/20 14:10:00
2.我下達了甚麼指令：把所有的免費不須API模型都丟進去 需API的模型提示使用者要填API 並且使用者自己填API 要付費也要提示 並且要有取的API連結及教學 依序是「免費不須API」「免費須API」「付費須API」 然後依照能力排序
3.AI的完整回覆：無
4.你做了甚麼事或修改：
- 更新了 AI 語音辨識的模型下拉選單，加入分群 (optgroup) 與詳細的能力說明與計費提示。
- 分類為：【免費不須API】(Transformers.js local)、【免費須API】(Groq Whisper Large V3)、【付費須API】(OpenAI API Whisper-1)。
- 在選單旁新增了「🔑 設定 API」按鈕 (僅在選擇需 API 的模型時出現)。
- 點擊設定會彈出專屬 Modal，讓使用者輸入並在 LocalStorage 儲存 Groq 和 OpenAI 的金鑰，並附上免費與付費提示及取得金鑰的教學連結。
- 實作了 Web API 的前端串接邏輯：在呼叫 API 模型時，會在瀏覽器內部直接將音訊壓縮為 Float32Array PCM -> WAV Blob，不需依賴肥大的 FFmpeg，並直接 POST 到對應的 API 端點，支援 OpenCC 繁簡轉換與段落即時轉譯。